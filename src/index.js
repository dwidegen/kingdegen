import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';   // ← path benar, cari App di folder yang sama (src)

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
